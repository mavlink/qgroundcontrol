"""Dependency-light shared utilities for QGC developer tools.

Import helpers from their defining modules (for example,
``from common.proc import run_captured``). Explicit imports keep each script's
runtime and sparse-checkout dependencies visible.
"""
