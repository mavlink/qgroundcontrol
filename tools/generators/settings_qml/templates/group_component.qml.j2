    ColumnLayout {
        Layout.fillWidth: true
        spacing: 0
        visible: {{ visible }}

        {{ component }} {
            Layout.fillWidth: true
{% for name, expr in (properties | default({})).items() %}
            {{ name }}: {{ expr }}
{% endfor %}
        }
    }
