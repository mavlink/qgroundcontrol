name: iOS

on:
  push:
    branches:
      - master
      - 'Stable*'
    tags:
      - 'v*'
    paths-ignore:
      - 'docs/**'
  pull_request:
    paths:
      - '.github/workflows/ios.yml'
      - '.github/actions/**'
      - 'deploy/ios/**'
      - 'src/**'
      - 'test/**'
      - 'CMakeLists.txt'
      - 'cmake/**'
      - 'tools/setup/*ios*'
      - 'tools/setup/*macos*'
  merge_group:
  workflow_dispatch:
  workflow_call:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/master' }}

permissions:
  contents: read
  id-token: write
  attestations: write
  actions: read

jobs:
  changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    # Skip path filtering for tags, manual triggers, and workflow_call
    if: github.event_name == 'push' || github.event_name == 'pull_request' || github.event_name == 'merge_group'
    outputs:
      src: ${{ steps.detect.outputs.src }}
      tests: ${{ steps.detect.outputs.tests }}
      ci: ${{ steps.detect.outputs.ci }}
    steps:
      - name: Detect Changes
        id: detect
        uses: ./.github/actions/detect-changes
        with:
          platform: ios

  setup:
    name: Setup Matrix
    runs-on: ubuntu-latest
    needs: changes
    # Run if: source/tests/ci changed, OR not a filtered event (tags, manual, workflow_call)
    if: |
      always() &&
      (needs.changes.result == 'skipped' ||
       needs.changes.outputs.src == 'true' ||
       needs.changes.outputs.tests == 'true' ||
       needs.changes.outputs.ci == 'true')
    outputs:
      matrix: ${{ steps.generate.outputs.matrix }}
      timeout_minutes: ${{ steps.generate.outputs.timeout_minutes }}
    steps:
      - name: Generate Matrix
        id: generate
        uses: ./.github/actions/generate-matrix
        with:
          platform: ios

  build:
    name: iOS (${{ matrix.name }})
    needs: [changes, setup]
    if: always() && needs.setup.result == 'success'
    runs-on: ${{ matrix.os }}
    timeout-minutes: ${{ fromJSON(needs.setup.outputs.timeout_minutes) }}

    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.setup.outputs.matrix) }}

    defaults:
      run:
        shell: bash

    env:
      PACKAGE: ${{ matrix.package }}

    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@v2
        with:
          egress-policy: audit

      - name: Checkout repo
        uses: actions/checkout@v6
        with:
          submodules: recursive
          fetch-depth: 0
          fetch-tags: true

      - name: Get build config
        id: config
        uses: ./.github/actions/build-config

      - name: Initial Setup
        uses: ./.github/actions/common

      - name: Install Qt for iOS
        uses: ./.github/actions/qt-ios
        with:
          version: ${{ steps.config.outputs.qt_version }}
          modules: ${{ steps.config.outputs.qt_modules }}
          ios-modules: ${{ steps.config.outputs.qt_modules_ios }}
          xcode-version: ${{ steps.config.outputs.xcode_ios_version }}
          build-type: Release
          cpm-modules: ${{ runner.temp }}/build/cpm_modules
          save-cache: ${{ github.event_name != 'pull_request' }}

      - name: Configure
        working-directory: ${{ runner.temp }}/build
        run: ${{ env.QT_ROOT_DIR }}/bin/qt-cmake -S ${{ github.workspace }} -B . -G Ninja
              -DCMAKE_BUILD_TYPE=Release
              -DQT_HOST_PATH="${{ env.QT_ROOT_DIR }}/../macos"
              -DQGC_STABLE_BUILD=${{ github.ref_type == 'tag' || contains(github.ref, 'Stable') && 'ON' || 'OFF' }}

      - name: Build
        uses: ./.github/actions/cmake-build
        with:
          build-dir: ${{ runner.temp }}/build
          build-type: Release

      - name: Attest Build with SBOM
        uses: ./.github/actions/attest-sbom
        with:
          subject-path: ${{ runner.temp }}/build/${{ env.PACKAGE }}.app
          subject-name: ${{ env.PACKAGE }}-ios
          scan-path: ${{ runner.temp }}/build

      - name: Upload Build File
        uses: ./.github/actions/upload
        with:
          artifact_name: ${{ env.PACKAGE }}.app
          package_name: ${{ env.PACKAGE }}
          upload_aws: 'false'
