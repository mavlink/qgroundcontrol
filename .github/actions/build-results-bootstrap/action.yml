name: Build Results Bootstrap
description: >-
  Sparse-checkout the scripts + composite actions consumed by build-results.yml
  jobs and install the Python scripts group. Single source for the
  sparse-checkout list previously duplicated across post-pr-comment and
  save-baselines (drift guarded by test_bootstrap_sparse_checkout.py).

inputs:
  python-groups:
    description: Dependency groups to install via setup-python
    required: false
    default: scripts

runs:
  using: composite
  steps:
    - uses: actions/checkout@v7
      with:
        sparse-checkout: |
          .github/actions/download-all-artifacts
          .github/actions/collect-artifact-sizes
          .github/actions/setup-python
          .github/scripts/check_baseline_ready.py
          .github/scripts/report_context.py
          .github/scripts/ci_bootstrap.py
          tools/_bootstrap.py
          .github/scripts/collect_artifact_sizes.py
          .github/scripts/collect_build_status.py
          .github/scripts/download_artifacts.py
          .github/scripts/generate_build_results_comment.py
          .github/scripts/templates/build_results.md.j2
          tools/common
          tools/qgc_tools
          tools/pyproject.toml
          tools/uv.lock
          tools/setup/install_python.py
        sparse-checkout-cone-mode: false
        persist-credentials: false

    - uses: ./.github/actions/setup-python
      with:
        groups: ${{ inputs.python-groups }}
