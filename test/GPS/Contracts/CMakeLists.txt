cmake_minimum_required(VERSION 3.25)
if(CMAKE_SOURCE_DIR STREQUAL CMAKE_CURRENT_SOURCE_DIR)
    project(QGCGPSContractsCheck LANGUAGES CXX)
    enable_testing()
    find_package(Qt6 REQUIRED COMPONENTS Core)
    add_subdirectory(../../../src/GPS/Contracts Contracts)
endif()

add_executable(QGCGPSContractsLinkTest GPSContractsLinkTest.cc)
target_link_libraries(QGCGPSContractsLinkTest PRIVATE QGCGPSContracts)
add_test(NAME GPSContractsLinkTest COMMAND QGCGPSContractsLinkTest)
set_tests_properties(GPSContractsLinkTest PROPERTIES LABELS "Unit;GPS;Contracts" TIMEOUT 10)

if(COMMAND add_qgc_test)
    target_sources(${CMAKE_PROJECT_NAME} PRIVATE GPSReceiverCapabilitiesTest.cc GPSReceiverCapabilitiesTest.h)
    add_qgc_test(GPSReceiverCapabilitiesTest LABELS Unit)
    target_sources(${CMAKE_PROJECT_NAME} PRIVATE GPSReceiverProfileTest.cc GPSReceiverProfileTest.h)
    add_qgc_test(GPSReceiverProfileTest LABELS Unit)
endif()
