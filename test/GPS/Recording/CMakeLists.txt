cmake_minimum_required(VERSION 3.25)
if(CMAKE_SOURCE_DIR STREQUAL CMAKE_CURRENT_SOURCE_DIR)
    project(QGCGPSRecordingCheck LANGUAGES CXX)
    enable_testing()
    find_package(Qt6 REQUIRED COMPONENTS Core Positioning Test)
    include(../../../src/GPS/cmake/GPSBuild.cmake)
    get_filename_component(gps_repository "../../.." ABSOLUTE)
    add_library(QGCRecordingTestLogging STATIC ../Replay/GPSParserLogging.cc)
    target_include_directories(QGCRecordingTestLogging PUBLIC "${gps_repository}/src/Utilities/Logging")
    target_link_libraries(QGCRecordingTestLogging PUBLIC Qt6::Core)
    add_subdirectory("${gps_repository}/src/Utilities/Parsing/Json" JsonValidation)
    foreach(component Contracts Scheduler Core Transport Recording)
        qgc_add_gps_component(QGCGPS${component} COMPONENT ${component} LOGGING_TARGET QGCRecordingTestLogging)
    endforeach()
endif()

add_executable(QGCGPSRecordingFormatTest GPSRecordingFormatTest.cc)
set_target_properties(QGCGPSRecordingFormatTest PROPERTIES AUTOMOC ON)
target_link_libraries(QGCGPSRecordingFormatTest PRIVATE QGCGPSRecording Qt6::Test)
add_test(NAME GPSRecordingFormatTest COMMAND QGCGPSRecordingFormatTest)
set_tests_properties(GPSRecordingFormatTest PROPERTIES LABELS "Unit;GPS;Recording" TIMEOUT 30)
