include(Libraries.cmake)
add_subdirectory(Integration)
add_subdirectory(Presentation)
foreach(
    component
    Core
    NMEA
    NTRIPSession
    NTRIPNetwork
    Models
    Positioning
    Connections
    RecordingController
)
    qt_extract_metatypes(QGCGPS${component})
endforeach()
