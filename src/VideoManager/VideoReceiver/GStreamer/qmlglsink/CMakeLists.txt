find_package(Qt6 REQUIRED COMPONENTS Core)
qt_add_library(qmlglsink STATIC)

include(GStreamer)
if(GST_FOUND)
	message(STATUS "GST Modules Found")
	include(BuildGstQtPlugin)
else()
	message(WARNING "GST Modules Not Found")
endif()
