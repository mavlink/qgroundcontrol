# This file sets some Git options for this repository. It overrides any
# user-specified Git settings.

# Set default behaviour, in case users don't have core.autocrlf set.
* text=auto

# Explicitly declare text files we want to always be normalized and converted 
# to native line endings on checkout.
# We set all of our source code files here.
*.cc  text
*.cpp text
*.h   text

# Some files are used by the Windows installer and so should always have Windows
# line endings.
*.nsi       text eol=crlf
license.txt text eol=crlf

# Denote all files that are truly binary and should not be modified.
# This is really just images for us.
*.png binary
*.jpg binary
