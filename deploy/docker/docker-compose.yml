# GENERATED by deploy/docker/gen_compose.py from variants.json — DO NOT EDIT.
# Re-generate after editing variants.json:  python3 deploy/docker/gen_compose.py
#
# Local builds (mirrors run-docker.sh; same image/target/build-args):
#   QGC_UID=$(id -u) QGC_GID=$(id -g) docker compose -f deploy/docker/docker-compose.yml run --rm --build ubuntu
#   ... run --rm --build fedora Debug        # override build type
# Pass your uid/gid as QGC_UID/QGC_GID so build artifacts aren't root-owned.

services:
  ubuntu:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile
      target: linux
    image: qgc-ubuntu-docker
    user: "${QGC_UID:-1000}:${QGC_GID:-1000}"
    environment:
      HOME: /tmp
      CLEAN_BUILD: "${CLEAN_BUILD:-0}"
      JOBS: "${JOBS:-}"
    volumes:
      - ../..:/project/source
      - ../../build:/project/build
    cap_add: [SYS_ADMIN]
    devices: ["/dev/fuse"]
    security_opt: ["apparmor:unconfined"]
  ubuntu-2204:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile
      target: linux
      args:
        BASE_REF: "ubuntu:22.04@sha256:0e0a0fc6d18feda9db1590da249ac93e8d5abfea8f4c3c0c849ce512b5ef8982"
        APT_EXTRA: "gcc-12 g++-12"
        PIP_CMAKE: "cmake>=3.25,<4"
        CC_PIN: "gcc-12"
        CXX_PIN: "g++-12"
    image: qgc-ubuntu-2204-docker
    user: "${QGC_UID:-1000}:${QGC_GID:-1000}"
    environment:
      HOME: /tmp
      CLEAN_BUILD: "${CLEAN_BUILD:-0}"
      JOBS: "${JOBS:-}"
    volumes:
      - ../..:/project/source
      - ../../build:/project/build
    cap_add: [SYS_ADMIN]
    devices: ["/dev/fuse"]
    security_opt: ["apparmor:unconfined"]
  ubuntu-2604:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile
      target: linux
      args:
        BASE_REF: "ubuntu:26.04@sha256:b7f48194d4d8b763a478a621cdc81c27be222ba2206ca3ca6bc42b49685f3d9e"
    image: qgc-ubuntu-2604-docker
    user: "${QGC_UID:-1000}:${QGC_GID:-1000}"
    environment:
      HOME: /tmp
      CLEAN_BUILD: "${CLEAN_BUILD:-0}"
      JOBS: "${JOBS:-}"
    volumes:
      - ../..:/project/source
      - ../../build:/project/build
    cap_add: [SYS_ADMIN]
    devices: ["/dev/fuse"]
    security_opt: ["apparmor:unconfined"]
  debian:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile
      target: linux
      args:
        BASE_REF: "debian:bookworm-slim@sha256:7b140f374b289a7c2befc338f42ebe6441b7ea838a042bbd5acbfca6ec875818"
    image: qgc-debian-docker
    user: "${QGC_UID:-1000}:${QGC_GID:-1000}"
    environment:
      HOME: /tmp
      CLEAN_BUILD: "${CLEAN_BUILD:-0}"
      JOBS: "${JOBS:-}"
    volumes:
      - ../..:/project/source
      - ../../build:/project/build
    cap_add: [SYS_ADMIN]
    devices: ["/dev/fuse"]
    security_opt: ["apparmor:unconfined"]
  fedora:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile
      target: linux
      args:
        BASE_REF: "fedora:44@sha256:6c75d5bf57cb0fa5aa4b92c6a83c86c791644496d9ac230de7711f5b8ec3b898"
        SETUP_BASE: "setup-base-dnf.sh"
        DEP_PLATFORM: "fedora"
    image: qgc-fedora-docker
    user: "${QGC_UID:-1000}:${QGC_GID:-1000}"
    environment:
      HOME: /tmp
      CLEAN_BUILD: "${CLEAN_BUILD:-0}"
      JOBS: "${JOBS:-}"
    volumes:
      - ../..:/project/source
      - ../../build:/project/build
  arch:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile
      target: linux
      args:
        BASE_REF: "archlinux:base@sha256:fe6972d4dc1f660c0c10f4c41b2de8986bab89e7e2955378f8beadb8ebcd7433"
        SETUP_BASE: "setup-base-pacman.sh"
        DEP_PLATFORM: "arch"
    image: qgc-arch-docker
    user: "${QGC_UID:-1000}:${QGC_GID:-1000}"
    environment:
      HOME: /tmp
      CLEAN_BUILD: "${CLEAN_BUILD:-0}"
      JOBS: "${JOBS:-}"
    volumes:
      - ../..:/project/source
      - ../../build:/project/build
  aarch64:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile
      target: linux-cross
    image: qgc-ubuntu-aarch64-docker
    user: "${QGC_UID:-1000}:${QGC_GID:-1000}"
    environment:
      HOME: /tmp
      CLEAN_BUILD: "${CLEAN_BUILD:-0}"
      JOBS: "${JOBS:-}"
    volumes:
      - ../..:/project/source
      - ../../build:/project/build
  android:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile
      target: android
    image: qgc-android-docker
    user: "${QGC_UID:-1000}:${QGC_GID:-1000}"
    environment:
      HOME: /tmp
      CLEAN_BUILD: "${CLEAN_BUILD:-0}"
      JOBS: "${JOBS:-}"
    volumes:
      - ../..:/project/source
      - ../../build:/project/build
