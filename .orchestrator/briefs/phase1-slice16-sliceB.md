<task>
Execute Slice B of plan .orchestrator/work/phase1-slice16-strings-audit/plan.md: extend `SprigCorePlugin::adjustSettingMetaData` so the `VideoSettings::videoSource` Fact enum hides 3DR Solo, Parrot Discovery, Yuneec Mantis G, Herelink AirUnit, and Herelink Hotspot — these vendor-specific presets are not relevant to the Sprig GCS product and must not appear in the Settings → Video → Source dropdown.

Files to modify:
- custom/src/SprigCorePlugin.cc — add a `videoSource` enum filter to `adjustSettingMetaData`
</task>

<context>
Repo: qgroundcontrol Sprig GCS fork (C++20 / Qt / QML, CMake + Ninja).
Plan path: .orchestrator/work/phase1-slice16-strings-audit/plan.md
Slice number: B (refactor — surface change only; no new behavior contract, no new tests)

Background:
- This is a custom/ fork; the Sprig product hides vendor-specific QGC presets via FactMetaData overrides in SprigCorePlugin, not by patching upstream. Zero entries are added to `UPSTREAM_PATCHES.md`.
- `VideoSettings` defines all `videoSource*` constants at `src/Settings/VideoSettings.h:53-64`. The five constants to remove from the dropdown are:
  - `videoSource3DRSolo`
  - `videoSourceParrotDiscovery`
  - `videoSourceYuneecMantisG`
  - `videoSourceHerelinkAirUnit`
  - `videoSourceHerelinkHotspot`
- The seven constants to KEEP are: `videoSourceNoVideo`, `videoDisabled` (yes, name lacks "Source" prefix — match it exactly), `videoSourceRTSP`, `videoSourceUDPH264`, `videoSourceUDPH265`, `videoSourceTCP`, `videoSourceMPEGTS`.
- `VideoSettings::settingsGroup` is the static `const char *` generated by `DEFINE_SETTING_NAME_GROUP()` — compare with `==` to a `QString`, not a string literal.
- The setting name to filter on is generated by `DEFINE_SETTINGFACT(videoSource)` — the matching name token is `VideoSettings::videoSourceName` (the macro generates a `static constexpr const char *` with the `Name` suffix). Verify the exact symbol exists in `src/Settings/VideoSettings.h` (or the macro definition in `src/Settings/SettingsGroup.h`) before writing — if the macro generates a different suffix, use whatever matches the existing pattern in upstream code.
- The FactMetaData mutator is `void FactMetaData::setEnumInfo(const QStringList &strings, const QVariantList &values)` at `src/FactSystem/FactMetaData.cc:582`. Use this single call (NOT separate `setEnumStrings`/`setEnumValues`).
- Read-accessors are `metaData.enumStrings()` and `metaData.enumValues()` (already const-correct, return by value, declared at `src/FactSystem/FactMetaData.h:121-122`).

Current state of the target method (custom/src/SprigCorePlugin.cc:117-139):

```cpp
void SprigCorePlugin::adjustSettingMetaData(const QString& settingsGroup, FactMetaData& metaData, bool& userVisible)
{
    QGCCorePlugin::adjustSettingMetaData(settingsGroup, metaData, userVisible);

    if (settingsGroup == AppSettings::settingsGroup) {
        if (metaData.name() == AppSettings::offlineEditingFirmwareClassName) {
            // ...
        } else if (metaData.name() == AppSettings::offlineEditingVehicleClassName) {
            // ...
        } else if (metaData.name() == QStringLiteral("indoorPalette")) {
            // ...
        }
    }
}
```

What to add: a new top-level `if (settingsGroup == VideoSettings::settingsGroup)` block at the bottom of the method that, when `metaData.name()` matches the video-source setting name, rebuilds the enum lists keeping only the seven allowed entries (filter by the value `QString`, not by display text — display strings are translated). Use `QSet<QString>` of the allowed value strings for O(1) membership tests, or a plain `QStringList::contains`.

Add `#include "VideoSettings.h"` near the other settings includes (alphabetical order is fine — match existing local style).

Constraints:
- REFACTOR/feature on the custom/ side only. No edits to anything under `src/**`, no edits to root or per-module `CMakeLists.txt`, no edits to `cmake/**`, no edits to `.clang-format`, `.clang-tidy`, `.pre-commit-config.yaml`, `.github/**`, `docs/**`, `AGENTS.md`, `CODING_STYLE.md`.
- The base-class call `QGCCorePlugin::adjustSettingMetaData(...)` must remain the first statement (it sets defaults the override builds on). Add your new block AFTER it.
- Do not change behavior of the existing `AppSettings` block.
- Preserve QGC invariants: no `Q_ASSERT` in production code, no hardcoded numeric literals where a constant exists, follow the surrounding clang-format style (already enforced).
- `UPSTREAM_PATCHES.md` must remain at zero active entries — verify after your edit.
- `build/` is already configured at `cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Release`.
- Do not touch `custom/custom.qrc` (merge-fragile per project memory).
</context>

<structured_output_contract>
Return a single Markdown block:

## summary
<1-3 lines: what changed, where>

## files_changed
- custom/src/SprigCorePlugin.cc — <+lines/-lines> — added VideoSettings::videoSource enum filter

## tests_run
- command: `cmake --build build --config Release --parallel && pre-commit run --files custom/src/SprigCorePlugin.cc`
- result: <build PASS/FAIL; pre-commit PASS/FAIL>

## followups
- <any constant-name surprises, FactMetaData API mismatches, or risks observed; empty if clean>
</structured_output_contract>

<default_follow_through_policy>
- Use `git mv` for renames so history is preserved (not applicable here, but follow if it ever is).
- After every change, run the verification command and confirm green before declaring done.
- If the symbol `VideoSettings::videoSourceName` doesn't exist (e.g., the macro generates a different suffix like `videoSourceSettingName`), use the existing-pattern name actually emitted by the project's `DEFINE_SETTINGFACT` macro — find it by checking other `metaData.name() == VideoSettings::...Name` comparisons elsewhere in the codebase (search via grep). Do NOT invent a new constant.
- If you discover the slice plan is wrong (e.g., `setEnumInfo` doesn't exist), STOP and report in `followups`. Do not freelance an alternative API.
- Stay inside the single-file scope: `custom/src/SprigCorePlugin.cc`. Do not "while we're here" edit other files.
</default_follow_through_policy>

<verification_loop>
1. Read `.orchestrator/work/phase1-slice16-strings-audit/plan.md` (just Slice B).
2. Read `src/Settings/VideoSettings.h` to confirm the exact symbol names and verify the seven allowed constants spelled above match the header.
3. Read `src/Settings/SettingsGroup.h` (or wherever `DEFINE_SETTINGFACT` lives) to confirm the exact `*Name` suffix the macro generates for setting names. Grep for `metaData.name() == VideoSettings::` in the existing codebase to see the established pattern.
4. Read `src/FactSystem/FactMetaData.h` lines 161 and 169 to confirm the `setEnumInfo` signature.
5. Apply the change to `custom/src/SprigCorePlugin.cc`.
6. Run `cmake --build build --config Release --parallel`. Must exit 0.
7. Run `pre-commit run --files custom/src/SprigCorePlugin.cc`. Must exit 0.
8. Return per the output contract.
</verification_loop>
